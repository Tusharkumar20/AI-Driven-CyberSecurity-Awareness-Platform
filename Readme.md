# Instructions

Place the .env files and the .json file shared with you in the respective folders.
Open two terminals for frontened and backend then execute the following commands on first run.
After that only "node server.js" and "npm run dev"

## Backend
cd frontend
npm install
node server.js

## Frontend
cd frontend
npm install
npm run dev